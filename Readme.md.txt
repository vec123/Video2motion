change the path to the dataset and run:

motion_extractor.py (extracts coordinates, saves npy, saves skeleton-motion as gif)


video2motion_helpers.py contains the corr2video functions (input- video  output- coordinates)

